class Weather {
  constructor(city, state) {
    this.apiKey = "006087c0a5ea31f70036041397f3554c";
    this.city = city;
    this.state = state;
  }

  //Fetch Weather from API
  async getWeather() {
    // const response await fetch(``);
  }
}
